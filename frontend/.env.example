# ============================================================
# FlashOrder — Frontend variables de entorno (EJEMPLO)
# Copia este archivo: cp .env.example .env
# Las variables deben tener prefijo VITE_ para ser accesibles
# en el navegador
# ============================================================

# URL base del backend API
VITE_API_URL=http://localhost:3000
